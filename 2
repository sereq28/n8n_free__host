version: '3.3'

services:
  n8n:
    image: n8nio/n8n:latest
    ports:
      - '5678:5678'
    environment:
      - N8N_PORT=5678
      - N8N_HOST=0.0.0.0
      - WEBHOOK_URL=https://${N8N_HOST}
    volumes:
      - /data:/home/node/.n8n
